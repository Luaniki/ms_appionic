import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ms_appionic_clase',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
